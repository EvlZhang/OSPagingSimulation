#include "fifo.h"
#include "page.h"
#include <queue>
#include <map>

using namespace std;
using namespace P;
void lru(int k,int references[]){
    //use map with least recently used field in Pages
    //or use linked list with order by frequency
}